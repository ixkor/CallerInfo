apply plugin: 'com.android.application'

android {
    compileSdkVersion 22
    buildToolsVersion "23.0.2"

    defaultConfig {
        applicationId "net.xkor.callerinfo"
        minSdkVersion 22
        maxSdkVersion 22
        targetSdkVersion 22
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
}
